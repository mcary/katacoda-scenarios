AWX it the open-source upstream project from which RedHat Ansible Tower is
dervied.  It puts a Jenkins-like web UI atop Ansible, allowing delegation and
tracking of Ansible playbook invocation, composition into workflows, and
credential management.

This scenario shows how to install AWX so you can poke at a running instance.
